# FIXED: .env files must not be committed — they contain dev localhost URLs
# that, once in git history, encourage committing real secrets in .env files.
# Vite vars are passed at build time via ARG in Dockerfile, not from this file.
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
