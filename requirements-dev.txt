# Include production dependencies
-r requirements.txt

# ── Testing ───────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0            # coverage reports
pytest-timeout==2.3.1        # per-test timeout guard
httpx==0.27.0                # async test client for FastAPI
faker==25.0.0                # realistic test data generation

# ── Code Quality ──────────────────────────────────────────
ruff==0.4.4                  # linter + formatter (replaces black + flake8)
mypy==1.10.0
pre-commit==3.7.1            # FIXED: was malformed "3.7.11.3"

# ── Notebook Utilities ────────────────────────────────────
ipywidgets==8.1.3            # FIXED: was malformed "8.-r requirements-train.txt"
tqdm==4.66.4