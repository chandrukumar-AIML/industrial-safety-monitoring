# ── Core ML ──────────────────────────────────────────────
ultralytics==8.2.18          # YOLOv8 — pinned for reproducibility
torch==2.3.0                 # CPU build for prod; Colab has its own
torchvision==0.18.0
numpy==1.26.4                # pinned — numpy 2.x breaks several deps
opencv-python-headless==4.9.0.80   # headless for server environments

# ── Tracking ─────────────────────────────────────────────
supervision==0.21.0          # ByteTrack lives here (ultralytics ecosystem)

# ── Explainability ───────────────────────────────────────
shap==0.45.1

# ── Backend ──────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0    # standard = websockets + httptools
pydantic==2.7.1
python-multipart==0.0.9      # for file upload endpoints
aiofiles==23.2.1
websockets==12.0
slowapi==0.1.9               # rate limiting middleware for FastAPI

# ── Database ─────────────────────────────────────────────
sqlmodel==0.0.18             # SQLite ORM built on SQLAlchemy + Pydantic
aiosqlite==0.20.0            # async SQLite driver (dev / fallback)
asyncpg==0.29.0              # async PostgreSQL driver (production)
psycopg2-binary==2.9.9       # sync psycopg2 for Alembic migrations

# ── Utilities ────────────────────────────────────────────
python-dotenv==1.0.1
loguru==0.7.2                # better logging than stdlib
Pillow==10.4.0               # Security fix for CVE-2024-28219
apscheduler==3.10.4

# ── RAG / Chatbot ─────────────────────────────────────────
langchain==0.2.16
langchain-community==0.2.16
langchain-chroma==0.1.4
chromadb==0.5.3
sentence-transformers==3.0.1
langchain-ollama==0.1.3
pypdf==4.3.1
# REMOVED: langchain-core==0.3.15 — let langchain install its compatible version

# ── Incident Reports ──────────────────────────────────────
reportlab==4.2.2
langchain-openai==0.1.25

# ── Drift Detection ───────────────────────────────────────
evidently==0.4.33
scipy==1.13.1
pandas==2.2.2

# ── Alert System ──────────────────────────────────────────
twilio==9.2.3
aiosmtplib==3.0.1
jinja2==3.1.4

# ── Pose Estimation ───────────────────────────────────────
mediapipe==0.10.14

# ── LangGraph Agent ───────────────────────────────────────
langgraph==0.2.35
langsmith==0.1.147
# REMOVED: langchain-core==0.3.15 — already handled by langchain dependency

# ── Worker Identity ───────────────────────────────────────
deepface==0.0.93
tf-keras==2.16.0

# ── LLM Manager (Enterprise multi-provider) ───────────────
httpx==0.27.2                # async HTTP for Groq, OpenRouter, OpenAI calls
# Optional: pip install razorpay  (only needed for billing/Razorpay)
# Optional: pip install groq      (not required — uses raw httpx)